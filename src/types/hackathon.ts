export interface Hackathon {
  id: number;
  title: string;
  organizer: string;
  startDate: string;
  endDate: string;
  mode: string;
  prizePool: string;
  participants: number;
  description: string;
  image: string;
}